# Mac only, Python 3.12: uv venv --python 3.12 .venv-train && uv pip install --python .venv-train/bin/python -r requirements-train.txt
torch
torchvision      # pretrained MobileNetV3 backbone (--arch mobilenet)
onnx
numpy<2.3
opencv-python<4.13
